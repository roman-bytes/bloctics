name: State Machine Screenshot Trigger
on:
  issue_comment:
    types: [created]
jobs:
  run-on-comment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Check for trigger phrase
        run: |
          if [[ "${{ github.event.issue.pull_request.url }}" != "" && "${{ github.event.comment.body }}" == *"Create state machine screenshot"* ]]; then
            echo "Creating state machine screenshot!"
            # add your code here to create the screenshot
          fi
